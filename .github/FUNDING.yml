# OpenWarden funding. Donations are 100% optional — OpenWarden is and stays free,
# no-subscription, local-only, with no paywalled features. See docs/FUNDING.md.
#
# NOTE: GitHub Sponsors must be ENABLED on the @modernn account for this "github:"
# entry to render the Sponsor button. That's a maintainer account action, not a
# repo file change. TODO(maintainer): enable Sponsors at github.com/sponsors.
github: [modernn]
